services:
  - type: web
    name: musician-backend
    runtime: node
    region: singaporeSingapore (Southeast Asia)
    plan: free
    rootDir: backend
    buildCommand: |
      npm install &&
      npx prisma generate &&
      npx prisma db push --accept-data-loss &&
      node -e "const { PrismaClient } = require('@prisma/client'); const p = new PrismaClient(); (async () => { await p.admin.upsert({ where: { email: process.env.ADMIN_EMAIL }, update: {}, create: { email: process.env.ADMIN_EMAIL } }); console.log('✅ Admin setup complete!'); process.exit(0); })();"
    startCommand: npm start
    healthCheckPath: /api/content/settings
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: musician-db
          property: connectionString
      - key: PORT
        value: 10000
      - key: ADMIN_EMAIL
        value: johnsonmbuguamuhabi@gmail.com
      - key: JWT_SECRET
        value: c75a6b62d207912f813ac27944be56df50deb5156dba1c019dadee806bdb79dc24f452e87ff704a143652ff0e95351b50af94c93f4c0949ee7c2b281e600d1b2
      - key: FRONTEND_URL
        value: https://bonke-me.netlify.app/
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        value: johnsonmbuguamuhabi@gmail.com
      - key: SMTP_PASSWORD
        value: pfqo ptxr fkab otny

databases:
  - name: musician-db
    databaseName: musician_69y3
    plan: free
    region: singapore